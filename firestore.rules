rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /days/{day} {
      allow read: if true;
      allow write: if true; // Temporarily allow public write for booking/cancellation
    }
  }
}
